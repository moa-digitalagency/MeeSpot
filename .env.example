#
# MeetSpot - Variables d'environnement
# MOA Digital Agency LLC
# Copiez ce fichier vers .env et remplissez avec vos vraies valeurs
#

# ===== CONFIGURATION DE LA BASE DE DONNÉES =====
# Option 1: URL complète (recommandé pour PythonAnywhere, Railway)
# DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: Variables individuelles (auto-détectées sur Replit)
# Si DATABASE_URL n'existe pas, l'URL sera construite automatiquement depuis :
# PGUSER=votre_user
# PGPASSWORD=votre_password
# PGHOST=votre_host
# PGPORT=5432
# PGDATABASE=votre_database

# Note: Sur Replit, ces variables sont créées automatiquement quand vous ajoutez PostgreSQL
# Sur PythonAnywhere/Railway, utilisez DATABASE_URL directement

# ===== SÉCURITÉ =====
# Clé secrète pour JWT et sessions Flask (générez avec: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=votre-cle-secrete-changez-moi-en-production

# Clé de chiffrement pour les données sensibles (générez avec: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
# IMPORTANT: Ne perdez JAMAIS cette clé! Toutes les données chiffrées deviendraient irrécupérables
ENCRYPTION_KEY=votre-cle-de-chiffrement-changez-moi

# ===== ENVIRONNEMENT =====
# Environment: development, production, ou testing
FLASK_ENV=production

# ===== NOTES DE DÉPLOIEMENT =====
# Pour PythonAnywhere:
#   1. Créez ces variables dans un fichier .env à la racine du projet
#   2. Le fichier passenger_wsgi.py chargera automatiquement ce fichier
#   3. Assurez-vous que le fichier .env n'est PAS dans .gitignore sur le serveur
#
# Pour Railway:
#   1. Ajoutez ces variables dans l'interface Railway (Settings > Variables)
#   2. Railway les injectera automatiquement au démarrage
#   3. Ne créez PAS de fichier .env sur Railway
#
# Pour Replit:
#   1. Utilisez les Secrets de Replit (lock icon dans la barre latérale)
#   2. Ajoutez chaque variable individuellement
#   3. Replit les injectera automatiquement
